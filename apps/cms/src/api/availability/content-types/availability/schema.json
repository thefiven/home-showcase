{
  "kind": "collectionType",
  "collectionName": "availabilities",
  "info": {
    "singularName": "availability",
    "pluralName": "availabilities",
    "displayName": "Availability",
    "description": "Blocked date range for a property, populated by the Airbnb iCal sync."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {
    "i18n": {
      "localized": false
    }
  },
  "attributes": {
    "property": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::property.property",
      "inversedBy": "availabilities"
    },
    "startDate": {
      "type": "date",
      "required": true
    },
    "endDate": {
      "type": "date",
      "required": true
    },
    "source": {
      "type": "enumeration",
      "enum": ["airbnb", "manual"],
      "default": "airbnb",
      "required": true
    },
    "externalUid": {
      "type": "string",
      "description": "iCal event UID, used for an idempotent upsert during sync."
    },
    "summary": {
      "type": "string"
    }
  }
}
